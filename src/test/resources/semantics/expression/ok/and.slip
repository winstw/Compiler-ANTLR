#import "file.map"

add as function(variable as integer): integer
do
    add := 5;
end

x as integer = 5;

main as function(): void
do

    result as boolean;
    result := (5 >= 3) and true;
    result := (x >= add(5)) and true;
    result := (100 >= (5 + x)) and true;
    result := (100 >= x) and true;

    dig();

end