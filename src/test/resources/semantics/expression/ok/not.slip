#import "file.map"

add as function(variable as integer): integer
do
    add := 5;
end

x as integer = 5;

main as function(): void
do

    result as boolean;
    result := not (5 < 3);
    result := not (x < add(5));
    result := not (100 < (5 + x));
    result := not (100 < x);

    dig();

end