#import "file.map"

int as integer = 2;
tab as integer[5];
struct as record
    int as integer = 2;
end;

main as function(): void
do
 left(int);
 left(tab[2]);
 left(struct.int);
 right(int);
 right(tab[2]);
 right(struct.int);
 up(int);
 up(tab[2]);
 up(struct.int);
 down(int);
 down(tab[2]);
 down(struct.int);
 jump(int);
 jump(tab[2]);
 jump(struct.int);
 dig();
end
